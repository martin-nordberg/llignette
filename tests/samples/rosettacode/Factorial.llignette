
fact: Package = (

    NaturalInt64 : Type = Int64 & >=0

    factorial = fn(n: NaturalInt64) -> NaturalInt64 => (
        result = 1 when n == 0,
        result = n * factorial(n - 1) when n > 1
    ).result

)
